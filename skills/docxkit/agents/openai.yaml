interface:
  display_name: "DocxKit"
  short_description: "Export prepared content as polished editable Word."
  default_prompt: "Use $docxkit to turn the available report content into a polished editable Word document."
